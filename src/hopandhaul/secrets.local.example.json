{
  "_comment": "Copy this file to secrets.local.json in this same folder and fill in real values. secrets.local.json is gitignored — never commit real keys. Every key here can also be set as an environment variable of the same name, which takes priority over this file. Both keys are OPTIONAL: weather, place search, ferry data, US fare anchors, and ground schedules all work with no keys at all.",

  "DUFFEL_API_KEY": "duffel_test_PLACEHOLDER",
  "GEOAPIFY_API_KEY": "PLACEHOLDER"
}
